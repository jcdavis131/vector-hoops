# Runtime deps for the offline pipeline gates in .github/workflows/ci.yml.
#
# Deliberately NOT the training stack — torch/scikit-learn are only needed for
# train_mtnn.py, which CI never runs (see LOCAL_GPU_HANDOFF.md: that work happens
# on a GPU box). Keeping this file to the gate's real needs is what lets CI stay
# a few seconds instead of pulling a 2 GB wheel.
#
# vector_core is NOT here: it is not on PyPI. It lives in jcdavis131/vector-hub
# under packages/vector-core, and ci.yml checks that repo out and installs it.
numpy>=1.24
pytest>=7.0
