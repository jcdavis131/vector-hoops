pipeline/.venv/
__pycache__/
pipeline/data/

.vercel
.cursor/

# Agent scratch: settings are local, and worktrees are nested git repos that
# would land in the index as unresolvable gitlinks.
.claude/

# Live agent session capture (generated by scripts/watch_session_monologue.py)
tasks/session-monologue.jsonl
tasks/session-monologue.md

# Local rollback snapshots (salary CSV, train matrix)
*.bak
*.prefix_bak
dist/
.vercel/
node_modules/
.vscode/
.DS_Store
